server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.28.37:3306/content_center?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # namespace: 43c6e64f-436c-4bd0-8cc4-f858880583f9
        cluster-name: ChengDu
    # 配置属性的方式打印feign的日志
    openfeign:
      client:
        config:
          # 全局配置时将UserCenter改成default即可
          UserCenter:
            connect-timeout: 5000 # 连接超时时间
            read-timeout: 5000
            logger-level: full
            # error-decoder: com.xiwei.SimpleErrorDecoder
            # retryer: com.xiwei.SimpleRetryer
            # request-interceptors:
             # - com.xiwei.FooRequestInterceptor
            contract: com.xiwei.SimpleContract
  application:
    name: ContentCenter

logging:
  level:
    # com.xiwei: debug
    com.xiwei.contentcenter.feignclient.UserCenterFeignClient: debug